flask
flask-cors
yfinance
pandas
numpy
scikit-learn
hdbscan>=0.8.33
scipy
arch>=6.3.0,<7.0.0
statsmodels
gunicorn
torch
# Optional (install separately if needed):
# lightgbm  # ~50MB - only needed for LGBM models (has RandomForest fallback)
# ripser  # ~20MB - only used for topological analysis (optional)
# persim  # ~5MB - only used with ripser (optional)
